# CI セキュリティ対策 ファクトチェック

## 対象

- `.github/dependabot.yml`
- `.github/workflows/codeql.yml`
- `.github/workflows/security.yml`
- `.github/workflows/scorecard.yml`
- `.github/workflows/deploy-pages.yml`
- `.github/CODEOWNERS`
- `Dockerfile`
- `requirements.txt`
- `SECURITY.md`
- `README.md`

## 確認日

2026-05-22

## 確認者

GitHub Copilot

## 根拠

- GitHub Docs: Dependabot options reference
- GitHub Docs: CodeQL workflow configuration options
- GitHub Docs: Dependency Review Action configuration
- GitHub official action repositories: `github/codeql-action`, `actions/dependency-review-action`
- OpenSSF Scorecard official action repository
- 各 workflow の YAML 読み込み確認

## 結果

OK。

CI で検出できる範囲を広げるため、依存関係更新、CodeQL、PR の依存関係レビュー、secret scan、GitHub Actions 静的解析、Dockerfile lint、OpenSSF Scorecard を追加しました。Docker base image と Python 依存は固定し、GitHub Actions の `permissions` は最小限に寄せ、`actions/checkout` の credential persistence を無効化しています。

## 検証

- `.github/dependabot.yml` と `.github/workflows/*.yml` の YAML 読み込み: 成功
- `git diff --check`: 成功
- Docker daemon が起動していないため、ローカルの `docker compose run --rm docs mkdocs build --clean --strict` は未完了

## 補足

Secret scanning、push protection、branch protection / rulesets は GitHub のリポジトリ設定で有効化する対策です。ファイル追加だけでは強制できないため、README に設定項目として明記しています。
