# 2026-05-22 依存固定と教材品質チェック ファクトチェック

## 対象ファイル

- `requirements.txt`
- `requirements.lock`
- `Dockerfile`
- `.dockerignore`
- `.github/workflows/security.yml`
- `README.md`
- `scripts/validate_docs.py`
- `docs/index_page.md`
- `docs/01_基礎文法/02_変数と型.md`

## 確認日時

- 2026-05-22 21:45

## 確認者

- GitHub Copilot

## 確認観点

- Docker と CI が推移的依存まで固定した `requirements.lock` を使うか。
- `requirements.txt` が人間編集用の直接依存として残っているか。
- `.dockerignore` が生成物、一時ファイル、作業記録を build context から除外しているか。
- MkDocs nav、全記事索引、ローカル Markdown リンクの同期漏れを検出できるか。
- README の確認コマンドがリポジトリの運用と矛盾していないか。
- `docs/01_基礎文法/02_変数と型.md` のコードコメントが、型の読みどころを説明しているか。

## 根拠

- `pip-compile requirements.txt --output-file requirements.lock --resolver=backtracking` の実行結果。
- `python scripts/validate_docs.py` の実行結果。
- `mkdocs build --clean --strict` の実行結果。
- 既存の `mkdocs.yml`、`docs/index_page.md`、README の運用方針。

## 結果

- OK

## 指摘

| 優先度 | 内容 | 対応 |
| --- | --- | --- |
| 中 | Docker Desktop が起動していないため、Docker Compose 経由の検証はローカルで実行できなかった。 | 同じ `requirements.lock` を入れた仮想環境で `mkdocs build --clean --strict` と `scripts/validate_docs.py` を確認した。 |

## 次回確認目安

- 2026-06
